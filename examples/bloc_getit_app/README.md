# bloc_getit_app

**flex_track** with **flutter_bloc** and **get_it**: after `FlexTrack.setup`, registers `FlexTrack.instance.client` in GetIt; `CounterCubit` calls `client.track`.

```bash
flutter pub get
flutter run
```
