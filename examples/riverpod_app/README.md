# riverpod_app

**flex_track** with **Riverpod**: `FlexTrack.setup` in `main`, then `Provider<FlexTrackClient>` returning `FlexTrack.instance.client` for manual `client.track` calls. Widget wrappers still use `FlexTrack.track` internally.

```bash
flutter pub get
flutter run
```
