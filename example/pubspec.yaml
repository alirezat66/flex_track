name: flex_track_example
description: Example app for flex_track package
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flex_track:
    path: ../
  firebase_core: ^2.32.0
  firebase_analytics: ^10.10.7
  mixpanel_flutter: ^2.3.4
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true 